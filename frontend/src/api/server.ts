const SERVER_HOST = 'http://localhost';
const SERVER_PORT = '8080';
const SERVER_URL = `${SERVER_HOST}:${SERVER_PORT}`;

const BACKEND_SERVER = {
  url: {
    signUp: `${SERVER_URL}/sign-up`,
  },
};

export { BACKEND_SERVER };
